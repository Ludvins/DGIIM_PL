programa() {
  var
    entero a;
    real b, c t
    buleano d y;
    caracter _e;
    entero f[3], g[3, 5];
  finvar

  entero[3, 5] func(int v, entero w entero x) {
    devolver v ** (v * v) + w;
  }

  entero func2() {
    var
      entero a;
    finvar
    a = 2;

    devolver a;
  }

  2func();

  a = -3;
  b = -3*2;
  c = 3/-2;
  d = verdadero;
  _e = 'h';
  f = {1, 2, -3};
  g = {1, 1, 1 ; 1, 1, 1; 1, 1, 1};
  f[a * 3] = 2;
  g[3*a-2] = 2;

  si (!(- a + 3 * 5) == -3) {
    salida "El valor de a es -3";
    g = func(3, 5);
  }
  otro
    entrada "algo", b, f[0];

  casos(a - 3) {
    caso 1:
    caso 2:
      salida "2";
      roto;
    caso 3: salida a-3;
    predeterminado:
  }

  si ((a * -3 == 4) && d || !d ^ d)
    mientras (!d)
      a = a + 3*-2;
}
