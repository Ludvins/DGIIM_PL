programa() {
  var
    entero n[2], m[2,2];
  finvar

  entero[2, 2] test(entero a[2], entero t[2, 2]) {
    devolver t * a[0];
  }

    n = {3, 2};
    m = {1, 2; 3, 4};
    m[1] = {5,6};

    salida n[1] + m[1, 0+1], "\n";

    m = test(n, m);
    salida m[0, 0], ",", m[0,1], ",", m[1,0], ",", m[1,1], "\n";
}
